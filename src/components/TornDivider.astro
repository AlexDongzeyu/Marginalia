---
/**
 * Section break. The hand-torn paper strips were removed by request; this keeps
 * quiet vertical breathing room between sections and draws nothing.
 * The `variant` prop is kept so existing usages stay valid.
 */
interface Props {
  variant?: "a" | "b" | "c";
}
---
<div class="section-break" aria-hidden="true"></div>
